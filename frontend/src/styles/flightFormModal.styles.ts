import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    inputStyle: {
        borderWidth: 1,
        borderColor: "#d7d7d7",
        borderRadius: 12,
        paddingHorizontal: 12,
        paddingVertical: 10,
        backgroundColor: "#fff",
    },
    pickerWrap: {
        borderWidth: 1,
        borderColor: "#d7d7d7",
        borderRadius: 12,
        overflow: "hidden",
        backgroundColor: "#fff",
    },
    buttonStyle: {
        flex: 1,
        borderRadius: 14,
        paddingVertical: 14,
        alignItems: "center",
    },
});
